const assetsPath = "assets/";

const worldScale = 3.5;
const worldWidth = 243 * worldScale;
const worldHeight = 418 * worldScale;

const bossSongHeight = worldHeight * 0.3;

const charScale = 1.3
const charWidth = 32 * charScale;
const charHeight = 34 * charScale;
